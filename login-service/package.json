{
  "name": "microservice03-rest_api",
  "version": "1.0.0",
  "description": "This microservice exposes an api endpoint that is used for logging in and out. The api endpoint runs on port 3001 and the database is on port 3002. The api endpoints are /api/login and /api/logout",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "amqplib": "^0.10.8",
    "bcrypt": "^6.0.0",
    "body-parser": "^2.2.0",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.16.0",
    "mongoose": "^8.15.1",
    "redis": "^5.1.1"
  },
  "devDependencies": {}
}
